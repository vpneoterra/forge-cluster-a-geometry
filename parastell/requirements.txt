# parastell/requirements.txt
# Service-specific requirements for the ParaStell stellarator geometry service.
# Common deps (fastapi, uvicorn, pydantic, psutil, prometheus-client, pint)
# are installed by forge-python-base. Only ParaStell-specific packages here.
#
# All versions pinned for reproducibility.
# ARM64 note: dagmc and pystell may require source builds — see BUILD_NOTES.md.

# ── CadQuery (ParaStell dependency) ──────────────────────────
cadquery==2.4.0
cadquery-ocp==7.7.2.1

# ── Scientific computing ──────────────────────────────────────
numpy==1.26.4
scipy==1.13.1

# ── VMEC / HDF5 data handling ────────────────────────────────
netCDF4==1.7.1
h5py==3.11.0
