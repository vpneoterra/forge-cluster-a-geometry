# paramak/requirements.txt
# Service-specific requirements for the Paramak fusion reactor CAD service.
# Common deps (fastapi, uvicorn, pydantic, psutil, prometheus-client, pint)
# are installed by forge-python-base. Only Paramak-specific packages here.
#
# All versions pinned for reproducibility.

# ── CadQuery (Paramak's core dependency) ─────────────────────
cadquery==2.4.0
cadquery-ocp==7.7.2.1

# ── Scientific computing ──────────────────────────────────────
numpy==1.26.4
scipy==1.13.1

# ── Paramak ──────────────────────────────────────────────────
paramak==0.8.3
